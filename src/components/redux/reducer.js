import {SHOW_MODAL,HIDE_MODAL} from './action'

export const reducer=(state, action)=>{
    switch (action.type) {
      case SHOW_MODAL:
        break;
      case HIDE_MODAL:
        break;
      default:
        return state
    }
  }